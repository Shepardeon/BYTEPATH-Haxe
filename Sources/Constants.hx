package;

class Constants {
	public static inline final PROJECT_NAME = "Bytepath";

	public static inline final UPDATE_RATE = 1 / 60;

	public static inline final GAME_WIDTH = 480;
	public static inline final GAME_HEIGHT = 270;

	public static inline final GAME_SCALE = 1;

	public static inline final WINDOW_WIDTH = GAME_WIDTH * GAME_SCALE;
	public static inline final WINDOW_HEIGHT = GAME_HEIGHT * GAME_SCALE;
}
